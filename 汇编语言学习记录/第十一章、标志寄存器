一、ZF标志
    flag（标志寄存器）的第六位，零标志位，他记录相关指令执行后，结果为0，ZF=1
    结果不为0，ZF=0
二、PF标志
    flag的第二位是PF，奇偶标志位，它记录指令执行后，结果的所有二进制位中的1的个数
    为偶数，PF=1，为奇数，PF = 0
三、SF标志
    第七位，符号标志位，结果是负数，SF = 1，结果为正数 ，SF = 0
四、CF标志，对于无符号而言的
    第0位，进位标志位
五、OF标志，对于有符号而言的
    溢出标志位，
六、adc指令
    进位加法指令，他利用了cf位上记录的进位值
    格式：adc 操作对象1，操作对象2
    功能：操作对象1 = 操作对象1 + 操作对象2 + cf

七、sbb指令
    带借位减法指令，他利用了CF位上记录的借位值
    格式：sbb 操作对象1，操作对象2
    操作对象1 = 操作对象1 - 操作对象2 - cf
八、cmp指令
    比较指令，功能相当于减法指令，只是不保存结果，仅仅改变标志寄存器的值
九、检测比较结果的条件指令
十、DF标志和串传送指令
    第十位，方向标志位，在串处理指令中，控制每次操作后SI，DI的增减
    DF=0，si,di递增反之递减
    movsb：一次传一个字节,movsw一次传一个字
    rep ：根据cx的值循环传送 和movsb，movsw配和使用
    cld：将DF设置为0
    std：将DF设置为1
十一、pushf和popf
    pushf将标志寄存器压入栈中
    popf将栈中数据弹出到标志寄存器
十二、标志寄存器在debug中的表示