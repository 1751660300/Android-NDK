1.源程序
    1.汇编指令：
        有对应的机器码的指令，可以被编译为机器指令，最终为cpu所执行。
    2.伪指令
        没有对应的机器码指令，不能被cpu所执行
        是由编译器来执行的指令，编译器根据伪指令来进行相关的编译工作；
        定义一个段：
            segment、ends是一对成对使用的伪指令，这是再写可以被编译器编译的汇编程序时
            必须要用到的一对伪指令，segment是开始，ends是结束。
        一个程序是由多个段组成，这些段被用来存放代码、数据、或者当作栈空间来使用。
        一个汇编程序必须有一个段；
        end：是一个汇编程序的结束的标记
        assume：含义为“假设”
    汇编源程序：伪指令，汇编指令
2.标号
    一个标号指代一个地址。
    codesg：放在segment前面作为一个段的名称，这个段的名称最终被编译、连接程序处理为一个段的段地址。